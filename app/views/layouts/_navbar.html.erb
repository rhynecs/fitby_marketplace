<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to "Home", root_path, class: "navbar-item" %>
      <a class="navbar-item">
        Listings
      </a>
    </div>
    

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if user_signed_in? %>
            <% if current_user.has_role? :admin %>
              <%= link_to 'Manage Users', users_path, class: "button is-warning" %>
            <% end %>
            <%= link_to 'Account', user_path(current_user.id), class: "button is-light" %>
            <%= button_to 'Log Out', destroy_user_session_path, method: :delete, class: "button is-light" %>
          <% else %>
            <%= link_to 'Register', new_user_registration_path, class: "button is-light" %>
            <%= link_to 'Log In', new_user_session_path, class: "button is-primary" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>